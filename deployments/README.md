# Deployments

Configurations to bootstrap AMP infrastructure.

- **NATS:** defined in root `docker-compose.yml`. For advanced options (cluster, JetStream streams), add a `nats.conf` here and mount it in compose.
